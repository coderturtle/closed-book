project_name: closed-book
project_title: "Closed Book"
classification: factory-output
status: active
lifecycle_stage: active
local_repo_path: /Users/hekton/Development/hekton/factory-output/closed-book
mind_palace_path: 20-projects/factory-output/closed-book
owner: coderturtle
created_date: 2026-07-14
promotion_target: none
privacy_boundary: public
github_account: coderturtle
github_remote_url: "git@github.com-coderturtle:coderturtle/closed-book.git"
vault_mutation_allowed: false
# version: applies to platform components (alpha|beta|v1.0|v1.x|v2.0) and factory outputs (v0|beta|v1.0|v1.x)
# Labs do not carry version numbers. Set by the Promotion Agent on first promotion; human confirms.
version: ""
default_agents:
  - claude
  - codex
related_projects: []

reproducibility:
  target: "blank Hekton machine"
  setup_mode: "scripted"
  bootstrap_script: "scripts/bootstrap-project.sh"
  prereq_script: "scripts/check-prereqs.sh"
  verify_script: "scripts/verify-project.sh"
  dry_run_supported: true
  destructive_actions_allowed: false
  env_file: ".env.example"
  local_assumptions_doc: "docs/local-assumptions.md"
  setup_doc: "docs/setup.md"
  reproducibility_doc: "docs/reproducibility.md"
  install_policy:
    prefer_homebrew: true
    prefer_documented_cli_commands: true
    avoid_opaque_installers: true
    require_manual_confirmation_for_unknown_installers: true

# Architecture map metadata — see 20-projects/hekton/architecture/
# Maturity is orthogonal to lifecycle_stage: 0 idea, 1 experimental, 2 working,
# 3 validated, 4 platform-grade, 5 factory-standard.
architecture:
  domain: unassigned
  capability_area: ""
  component_type: factory-output
  maturity_level: 1
  maturity_label: experimental
  maturity_set_by: human
  maturity_basis: asserted
  maturity_date: 2026-07-14
  target_state_relevance: experimental
  depends_on: []
  enables: []
  consumes: []
  promotion_candidate: false
  promotion_target: none
  next_architecture_step: ""
  last_validated: 2026-07-14
  evidence_links: []
  risks: []
  owner: coderturtle
notes:
  - Learn Anthropic's Claude Developer certification material (Foundations to Professional) the agent-native way: hands-on Claude Code exercises gated by closed-book practice checkpoints against the published exam blueprint.

deployment:
  pattern: github-pages-dns
  subdomain_fqdn: closed-book.coderturtle.io
  root_domain: coderturtle.io
  pages_host: coderturtle.github.io
  repo: coderturtle/closed-book
  create_domain_verification: true
  human_confirmed: true
  hosted_zone_id: Z3DLQAZ6G5LV63
  record_fqdn: closed-book.coderturtle.io
  verification_record_fqdn: _github-pages-challenge-coderturtle.closed-book.coderturtle.io
  applied_date: 2026-07-18
  # Recorded after a real `terraform apply` in agentic-infra-lab's
  # github-pages-dns pattern (hosted zone Z3DLQAZ6G5LV63). Both records
  # confirmed live via `dig` against real public resolvers.
  # GitHub domain verification (step 11b, browser-only, no API) is the one
  # remaining step, pending on the human completing it at
  # github.com/settings/pages now that the TXT record resolves publicly.

