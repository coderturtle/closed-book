# Next Actions: Closed Book

## Immediate

- [x] Build module/deliverables skeleton (10 module READMEs + brand layer), per `docs/workshop-design.md`'s arc
- [x] Stand up build-log/Pages site skeleton (Astro-on-Pages, adapted from Borrow Native's pipeline)
- [x] Coachgremlin: author Module 01 for real (exercise, rubric, deterministic checker, dry run) — done 2026-07-15
- [x] Doubt-driven-development review of Module 01 (fresh Claude subagent + Codex + Fable replan) — done 2026-07-15, findings and full remediation recorded in `docs/decisions.md`
- [x] Author Module 01's closed-book checkpoint (`checkpoint.md`, 12 questions, full Domain 3 coverage) — done 2026-07-15, was previously missing entirely
- [x] Rewrite `verify_module_01.py` in Python, fixing portability/scoping/path-escape bugs — done 2026-07-15
- [x] A fourth dry-run attempt for Module 01 that passes the deterministic tier but is conceptually weak — done 2026-07-15 (`weak-conceptual-attempt`), confirms rubric criteria 2-4 catch what the checker structurally can't
- [x] Coachgremlin: author Module 02 (Prompts and Structured Output), continuing the `resolve` project — done 2026-07-15, both tiers built together from the start this time.
- [x] Wire the cumulative-gate convention for real (`verify_module_02.py` imports and calls `check_module_01`) — done 2026-07-15, isolation-tested (fails correctly when Module 01's config is absent, before pytest even runs).
- [x] Doubt-driven-development review of Module 02 (fresh Claude subagent + Codex with live web search + Fable replan) — done 2026-07-15. Dominant finding: the exercise interface never actually required the prompt/few-shot artifact 2 rubric criteria graded. Fixed by adding `build_extraction_prompt`/`FEW_SHOT_EXAMPLES` as real exercise deliverables, not by weakening the rubric. Full record in `docs/decisions.md`.
- [x] A 4th dry-run attempt for Module 02 that passes the full test suite but is conceptually weak — done 2026-07-15 (`weak-few-shot-attempt`, clean-only few-shot examples), confirms rubric criterion 3 catches what the 14-test deterministic suite structurally can't.
- [ ] Module 02 rubric criterion 4 (documented reason `refund_amount_cents` is nullable) still has no constructed dry-run attempt that isolates it in particular — logged in `runs/2026-07-15-module-02-dry-run/grading.md`'s "not yet validated" note.
- [x] Coachgremlin: author Module 03 (Designing Tools and MCP Interfaces), continuing `resolve` — done 2026-07-15, both tiers together, applying Modules 01-02's lessons proactively (docstring-presence check designed deterministic from the start, not found conceptual-then-fixed after a review).
- [x] Doubt-driven-development review of Module 03 (fresh Claude subagent + Codex with live repo exploration + Fable replan) — done 2026-07-15. Found the cumulative-gate chain into Module 04 was already broken (inconsistent `check_module_NN` return shapes, no `check_module_03`) and, more seriously, that the checker trusted a learner-editable test file (a submission could weaken/delete `tests/test_tools.py` and pass trivially) — the same hole existed in Module 02's checker too, fixed in both. Full record in `docs/decisions.md`.
- [ ] Module 03 rubric criterion 4 (process_refund's defense-in-depth reasoning documented as deliberate) has no constructed dry-run attempt isolating it specifically, distinct from the now-deterministic ordering test — logged in `runs/2026-07-15-module-03-dry-run/grading.md`.
- [x] Coachgremlin: author Module 04 (Agentic Loops and Multi-Agent Orchestration), continuing `resolve` — done 2026-07-15, both tiers together, applying Modules 01-03's lessons proactively (a conceptually-weak-but-deterministically-passing attempt constructed before shipping, not found afterward by an external review).
- [x] `backend.py`'s stated deferral resolved: Module 04 does not extend `Backend` into a stateful, mutating protocol — its exercise is orchestration (the loop + hook), not refund persistence/idempotency, which stays Module 03's already-resolved territory. See `docs/decisions.md`.
- [x] Doubt-driven-development review of Module 04 (fresh Claude subagent + Codex + Fable critique of the remediation) — done 2026-07-15/16. Dominant finding: `verify_before_refund_hook` blocked on "any `get_customer` success" without binding to *which* customer was verified — a session verifying customer A could refund customer B, undetected by any of the 5 originally-constructed attempts. Fixed with `SessionState.verified_customer_id`, 5 new tests (12→17), plus 4 smaller real issues (test-double `backend`-omission loophole, a factually-wrong checkpoint answer on session forking, a stale cross-module contradiction in Module 03's README, a checkpoint/README pass-threshold mismatch). Full record in `docs/decisions.md`.
- [ ] Module 04 rubric criterion 3 (rejection message quality beyond the deterministic keyword check) has no constructed dry-run attempt isolating it specifically, distinct from `thin-docstring-attempt`'s reasoning-only gap — logged in `runs/2026-07-15-module-04-dry-run/grading.md`.
- [ ] No attempt constructs a long, realistic multi-turn session exercising the hook under drift (many tool calls across several unrelated customers in one session) — the current 17 tests are short, deliberately minimal scripted scenarios, flagged in `runs/2026-07-15-module-04-dry-run/grading.md`'s "not yet validated" note.
- [x] Coachgremlin: author Module 05 (Context and Reliability at Scale), continuing `resolve` — done 2026-07-16, both tiers together. `src/context.py`'s `CaseFacts`/`update_case_facts`/`should_escalate` cover Task Statements 5.1, 5.2, 5.5, 5.6; 5.3 (multi-agent error propagation) and 5.4 (large-codebase context management) have no `resolve`-specific artifact yet and are checkpoint-only. `check_module_04` chained for real.
- [x] Doubt-driven-development review of Module 05 (fresh Claude subagent + Codex + Fable critique of the remediation) — done 2026-07-16. Dominant finding: the test suite tested `customer_id` far more rigorously than the structurally-identical `order_id`/`refund_amount_cents` mappings — a request-vs-result mixup, a hollow conflict entry, or a silently-overwritten conflict on either field all passed the original 20 tests. Fixed with symmetric coverage (20→28 tests), plus a shallow-copy mutation hole, an unenforced `should_escalate` signature guarantee, a dead `issue_summary` field, an untested iteration boundary, and a checkpoint overclaim. The Fable critique of that remediation then found the symmetric-coverage fix was itself only half-symmetric (conflict entries missing `source_tool` assertions for two of three fields, an error-path key-collision gap on `lookup_order`, refund conflicts not asserting most-recent-wins, no no-conflict-on-repeat test for refund amounts) — fixed, growing the suite to 30 tests. Every fix in both rounds verified against a live constructed counter-implementation, not just re-read for plausibility. Full record in `docs/decisions.md`.
- [x] Coachgremlin: author Module 06 (Foundations Capstone), continuing `resolve` — done 2026-07-16. `src/session.py` (a real integration of Modules 02, 04, 05) ships with 2 seeded defects spanning those modules; the provided 5-test suite fails 2/5 against the exact shipped file. Closed-book mock exam authored as an original 6-scenario/4-drawn pool (42 questions, 720/1000 to pass), matching the real exam's structure. `check_module_05` chained for real. This closes Part 1 (Architect Foundations) of the workshop's arc in full.
- [x] Doubt-driven-development review of Module 06 (fresh Claude subagent + Codex + Fable critique of the remediation) — done 2026-07-16. Dominant findings: every one of the checkpoint's 42 answer-key entries named option A as correct (fixed via a mechanically-verified option shuffle); the "correct" reference implementation itself never escalated when the hook repeatedly rejected a call, contradicting this project's own fail-closed default (fixed uniformly across all six copies of `session.py`, a genuine base-design gap, not one of the two seeded pedagogical defects — test suite grew from 5 to 6). Also fixed: 3 scenarios renamed to reduce resemblance to the real exam's own published scenarios, two coverage gaps (Task Statement 5.4 absent from the pool; Domain 3/4 representation skew) disclosed explicitly, and a Scenario B stem mismatch. The Fable critique then found the new 6th test didn't check against an over-eager escalation mutant, and that the scenario renaming left stale vocabulary in several distractors — both fixed and re-verified against constructed mutants. Full record in `docs/decisions.md`.
- [x] Part 2 shared-project decision resolved via `AskUserQuestion` (two rounds) — new shared system, not a `resolve` continuation; named **Foundry**, an internal AI platform team's own product, chosen specifically because it naturally covers all 7 CCAR-P Professional domains (Domain 7, Developer Productivity & Operational Enablement, above all). Full rationale in `fixtures/foundry/SPEC.md` and `docs/decisions.md` (2026-07-17 entries).
- [x] Coachgremlin: author Module 07 (Designing the Solution: Architecture, Models & Context Strategy), opening Part 2 on `fixtures/foundry/` — done 2026-07-17, both tiers together. `src/ticket_triage.py` (`build_triage_prompt`/`classify_ticket`, a deliberately cache-friendly single-turn classifier) plus a real architecture decision record, checked in one gate by `scripts/verify_module_07.py` (12 code tests + regex-based ADR structural check). Cumulative-gate scope deviation stated explicitly: Part 2 checkers do not chain back through Module 06/`resolve`. Checkpoint (`checkpoint.md`) cites CCAR-P Domain 1/2 by named objective, not invented task-statement numbers — no granular numbered breakdown exists in `docs/workshop-design.md` for these domains, confirmed by search rather than assumed.
- [x] Doubt-driven-development review of Module 07 (fresh Claude subagent + Codex + Fable critique of the remediation) — done 2026-07-17. Dominant finding was project-wide, not Module-07-scoped: all six checkers (`verify_module_02.py`–`verify_module_07.py`) ran pytest with `cwd=target`, letting a submission bypass the entire test suite via either a shadow `pytest.py` (Codex's finding) or `os._exit(0)` at import time (found by a Fable-model critique of the first fix) while still reporting a clean pass — both empirically confirmed live against Module 04's checker with a real known-broken implementation, both now closed in all six checkers (neutral temp-dir execution + expected-pass-count verification, not just a zero exit code). Also closed 8 narrower test-suite precision gaps in Module 07's own suite (12 → 20 tests). Full record in `docs/decisions.md` and `runs/2026-07-17-module-07-dry-run/grading.md`.
- [x] Full regression across all 25 Part 1 dry-run attempts (Modules 01-06) — done 2026-07-17, re-run twice (once after adding `fixtures/foundry/`, once after the systemic checker fix), zero regression both times, all exact prior pass/fail patterns reproduced.
- [x] Coachgremlin: author Module 08 (Building and Proving It: Integration, Evaluation & Optimization), continuing `foundry`, chaining `check_module_07` for real — done 2026-07-17. Scenario: the Platform Docs team's documentation Q&A system (a genuine RAG use case, unlike Module 07's classifier). Two deliberately independent deliverables: `src/doc_qa.py` (ships mostly working, one seeded staleness defect in `refresh_index` to diagnose and fix — anchored to the exam guide's own Sample Question 3) and `src/evaluation.py` (build-from-stub `evaluate`/`compare_top_k`).
- [x] Doubt-driven-development review of Module 08 (Claude subagent + Codex + Fable critique of the remediation, two full review cycles) — done 2026-07-17. Stage 1/2 found and fixed 8 real gaps (test suite 21→32), most severe an `evaluate` bypassing real retrieval and a `refresh_index` mutation/shared-state bug, both empirically confirmed live. Stage 3 (Fable) found the remediation substantially real but not fully complete — most severe: `evaluate` ignoring its own `top_k` parameter while `compare_top_k` reimplemented it independently, confirmed live to pass all 32 tests; fixed with 2 more tests (32→34). One Codex claim (a fake-summary-line checker bypass) was tested live and refuted — pytest's own output capturing during collection swallows the print before `os._exit(0)` can flush it. Full record in `docs/decisions.md` and `runs/2026-07-17-module-08-dry-run/grading.md`.
- [x] Coachgremlin: author Module 09 (Shipping Responsibly: Governance, Stakeholders & Team Enablement), continuing `foundry`, chaining `check_module_08` for real — done 2026-07-17. Doesn't open on a new team's problem; asks what has to happen before Module 08's own `doc_qa` system ships. Three deliverables: `src/governance.py` (human-in-the-loop gate), `docs/shipping-readiness-review.md` (prose), `.claude/` team tooling config.
- [x] Doubt-driven-development review of Module 09 (Claude subagent + Codex) — done 2026-07-17, the most severe single finding of any module to date: every original test happened to place the sensitive marker in both the query AND the retrieved chunk, so a submission checking the query instead of chunk content passed all 14 tests while completely defeating the module's safety property — confirmed live. Codex then found `review_reason` could leak the actual sensitive content back to the caller, also confirmed live. Both fixed (test suite 14→17), plus a shared bare-directory-match bug in `verify_module_01.py`'s own checker (found via reuse in Module 09) fixed in both checkers at once.
- [ ] Stage 3 (Fable-model critique of the remediation) did not run for Module 09 — Fable was unavailable this session ("Usage credits are required for this model"). Proceeded without it on explicit user direction. Worth a follow-up critique pass against the current, already-twice-reviewed files once Fable access is restored.
- [x] Coachgremlin: author Module 10 (Professional Capstone: Sit-Ready for CCAR-P), continuing `foundry`, chaining `check_module_09` for real — done 2026-07-17, closes Part 2 (Architect Professional) and this workshop's whole content arc in full. Purely-written deliverable (`docs/capstone-architecture-defense.md`, defending Foundry's own three systems against a seeded stakeholder consolidation objection) plus a 42-question, 6-scenario, 720/1000 mock exam covering all 7 CCAR-P domains, matching Module 06's real-exam-scale precedent.
- [x] Doubt-driven-development review of Module 10 (Claude subagent + Codex + Fable critique of the remediation, three full review cycles) — done 2026-07-17. Self-caught before any review: an all-one-letter answer bug and a domain mislabel, plus a self-inflicted answer-shuffle corruption bug found and reverted via reconstruction (no git history existed to revert to). Stage 1: 9 findings, 2 checker exploits and 2 reference-defense gaps fixed, 1 refuted as noise (D4/F3's domain label matches Module 08's own established precedent). Stage 2 (Codex): found the reshuffled answer key formed a literal repeating A,B,C,D cycle and both multi-response questions shared the same correct pair — fixed with a re-shuffle adding a per-scenario balance constraint. Stage 3 (Fable): found the Stage 1+2 remediation itself had gaps, most seriously a checker fix that would have unfairly failed honest writing using negation-shaped language — fixed via a narrower, curated self-denial-phrase proximity check instead of whole-sentence negation exclusion. Fable also found a project-wide, pre-existing construct-validity issue (every distractor across all 42 questions used absolute-quantifier tells) — per explicit user direction, all 42 questions' distractors rewritten (not just disclosed) to close it. Full record in `docs/decisions.md` and `runs/2026-07-17-module-10-dry-run/grading.md`.
- [ ] Workshop's full 10-module content arc is now complete (Modules 01-10, both Part 1 and Part 2). Next real milestone per the dogfooding commitment: sit the real CCAR-P exam (see "Later" section below).
- [ ] The expected-pass-count check (`f"{expected} passed" in output`) is itself a coarse proxy — it doesn't parse pytest's structured output, just the summary line's substring. Not pursued further given the value at this scale, but worth a note if this project ever needs machine-parsed test results elsewhere (e.g. `pytest --json-report`).
- [ ] `verify_module_10.py`'s risk-vocabulary check has one disclosed, unfixed gap: a purely affirmative, non-negated buzzword mention (e.g., a defense quoting a stakeholder's memo naming the right words without ever engaging them) can still pass — no lexical check reliably distinguishes real engagement from name-dropping. Backstopped by the module's own conceptual rubric, same precedent as every other prose checker in this project; not pursued further given the coarse-proxy discipline already established.
- [ ] A real learner (not just constructed attempts) needed before this workshop's two-tier gate hypothesis has any evidence beyond self-constructed dry runs
- [ ] `fixtures/resolve/requirements.txt` (pytest) has no documented venv-setup step in the module READMEs yet beyond `SPEC.md`'s "Running it" section — consider whether this belongs in the top-level README's prerequisites too, before Module 03 adds more dependencies

## This Week

- [ ] Deferred from the 2026-07-14 Review Panel pass (`docs/review-panel/2026-07-14-initial-design.md`), real open questions not fixable as design-doc text:
  - [ ] Concrete grading-authority mechanism for the hands-on tier in a self-paced, facilitator-less repo (self-check checklist vs. something else — currently stated as "self-graded via checklist," needs a real decision on whether that's sufficient)
  - [ ] Prerequisite-enforcement mechanism (currently prose-only reasoning, no technical gate preventing skipping ahead)
  - [ ] Time-per-module estimate (needs real content to estimate honestly)
  - [ ] Module 01 skip-ahead path for learners already fluent in Claude Code configuration
  - [ ] README's learner-facing rewrite (lead with the hands-on payoff, not the certification framing; split internal Hekton framing into `docs/maintainers.md`) — belongs to the Deliverables & branding pass, not a standalone fix
- [ ] Re-run the Workshop Review Panel (or a scoped subset) once Deliverables & branding produces real module content — several findings can only be checked against actual exercises

## Later

- [ ] Schedule and sit the real CCA-F exam once Modules 01-06 exist (dogfooding commitment, `docs/workshop-design.md`)
- [ ] Schedule and sit the real CCAR-P exam once Modules 07-10 exist
- [ ] Get a human to enable GitHub Pages and trigger the first real deploy (human-confirmed gate, matches Terminal Velocity/Borrow Native precedent)
- [ ] Confirm the first push to the public GitHub remote (`git push -u origin main`) — currently created but not pushed, per the Human Gate
- [ ] Factory-wide: audit whether other Hekton-scaffolded repos' `CLAUDE.md` files have the same unconditional "close every session by running end-session.sh" instruction that caused a read-only Codex review agent to attempt writing during this workshop's doubt-driven-development pass (fixed locally here 2026-07-15; not fixed at the `~/hekton` scaffold-template level, out of scope for this repo's own session)
- [ ] The 2026-07-15 fix (scoping the closeout instruction to sessions that made changes) didn't fully close the gap: Codex's Module 04 doubt-driven-development review attempted `bash ~/hekton/scripts/end-session.sh` again on 2026-07-16 despite making zero file changes — it failed safely (read-only sandbox, missing `--title` in non-interactive mode) but reasoned it should attempt the closeout anyway. Prose scoping in `CLAUDE.md` doesn't reliably bind an external CLI's own judgment the way it does a Claude Code session; worth a harder technical gate (e.g., the script itself refusing to run with no working-tree changes) rather than relying on instruction text alone. Not fixed here — same out-of-scope reasoning as the entry above.
