# Decisions: Closed Book

## ADR Log

| Date | Decision | Rationale |
|---|---|---|
| 2026-07-14 | Initial scaffold as factory-output | Learn Anthropic's Claude Developer certification material (Foundations to Professional) the agent-native way: hands-on Claude Code exercises gated by closed-book practice checkpoints against the published exam blueprint. |
| 2026-07-14 | Naming pass complete: renamed to **Closed Book** | Chosen by coderturtle from naming-agent candidates (Proof of Work, Closed Book, Blueprint Native, No Hints), GitHub-slug-checked first. GitHub repo, local dir, git remote, repo-local mind-palace mirror, and vault card all renamed to match; vault mutation explicitly authorized for this rename. |
| 2026-07-14 | **Correction:** target redirected from "Claude Developer, Foundations → Professional" to **Claude Architect, Foundations → Professional** | Research found no "Claude Developer – Professional" exam exists — only 4 live exams as of today: Associate–Foundations, Developer–Foundations, Architect–Foundations, Architect–Professional. Developer has Foundations only; Architect is the only track with both levels. coderturtle chose to redirect the whole track to Architect (explicit tradeoff named: less pure hands-on-coding, more design/governance framing) rather than stay on Developer-only or bridge into a different track. |
| 2026-07-14 | Module arc anchored to Anthropic's own primary-source exam guide PDFs, not third-party prep-site summaries | An early secondary-source search returned identical domain/weight lists recycled across different exam-name pages (SEO content, possibly copied or scraped) — unreliable. Fetched and read Anthropic's official *Claude Certified Architect – Foundations* and *– Professional* exam guide PDFs in full instead. |
| 2026-07-14 | 10-module arc (6 Foundations + 4 Professional, including two capstones), not the originally-scoped 8 (4+4) | CCA-F has 5 real domains, CCAR-P has 7 — forcing 4+4 would misrepresent either the exams' real weights or their dependency order. Foundations gets one module per domain (5) plus a capstone (6); Professional's 7 domains cluster into 3 real thematic groups (design / build & validate / govern & deliver) plus a capstone (4). See `docs/workshop-design.md`'s module arc tables. |
| 2026-07-14 | Two-tier module gate: hands-on Claude Code artifact, then closed-book no-AI practice checkpoint | Named design tension: the workshop's method (agent-native) is the opposite of the real exam's condition (proctored, closed-book, no AI). Proposed resolution is not either/or but both, in sequence, per module — see `docs/design-tension.md`. **Stated hypothesis, not yet evidenced** — no module content exists yet and neither real exam has been sat. Practice questions are always written originally against the published blueprint, never copied from real/leaked exam content or Anthropic's own official sample questions. |
| 2026-07-14 | Dogfooding commitment: coderturtle will personally sit the real CCA-F exam after Modules 01-06 exist, and CCAR-P after Modules 07-10 exist | Same discipline as Borrow Native's Ardan Labs commitment — stated intent, not yet evidenced. |
| 2026-07-14 | Build-log/Pages site adapted from Borrow Native's Astro starter (Content Layer API, base-aware links, `deploy-pages` Action) | Same pipeline validated twice already (Terminal Velocity, Borrow Native) — reuse rather than reinvent. Locally validated: `npm run build` and `astro check` both clean, base-path links confirmed in built HTML (`/closed-book/` prefix present on every internal href). Inherited 4 npm vulnerabilities (3 low, 1 high) logged as RISK-0002, deferred to before the first real deploy per Borrow Native's own precedent. First live deploy remains human-confirmed (`workflow_dispatch` only). |
| 2026-07-15 | Modules 01-06 (Part 1, Architect Foundations) build one shared project, `resolve` | Modeled directly on CCA-F's own Scenario 1 (a customer support resolution agent with `get_customer`/`lookup_order`/`process_refund`/`escalate_to_human`) rather than independent per-module fixtures — same shape Borrow Native adopted, chosen because CCA-F's five domains are additive toward one real system. Part 2 (07-10) does not extend it; left as an open question for whoever authors that content. |
| 2026-07-15 | Module 01 authored for real, with a completed dry run | First Coachgremlin content pass. Deterministic tier is a scripted checker (`scripts/verify-module-01.sh`) validating Claude Code configuration structure *and* that path-scoped rule globs actually resolve against real files, not just exist. Dry run (`runs/2026-07-14-module-01-dry-run/`) constructed 3 attempts (naive/correct/broken-glob); the checker discriminated correctly across all three and caught a real bug in itself (missing `shopt -s globstar`) before it could ship a false negative to a learner. |
| 2026-07-15 | **Doubt-driven-development review of Module 01, before treating it as done** | Two independent adversarial reviews (a fresh-context Claude subagent, and Codex CLI, both given ARTIFACT+CONTRACT only, never the author's own reasoning) plus a Fable-model subagent tasked with critiquing and replanning given both reviews' findings. Both reviews found substantial, largely non-overlapping, mostly-corroborated real issues — not doubt theater. Full finding list and reconciliation kept in this session's transcript; headline: the closed-book checkpoint tier didn't exist for any module despite being the workshop's namesake feature, and the bash checker had several real bugs (non-portable `globstar`, quoted-only YAML matching, a scoping check weak enough to pass a rule scoped to the wrong directory, no protection against a glob pattern escaping the project directory). |
| 2026-07-15 | **Remediation applied**, all before Module 02 | (1) Authored `modules/01-configuring-claude-code/checkpoint.md`, 12 originally-written closed-book questions covering the full CCA-F Domain 3 blueprint (3.1-3.6) — the hands-on tier's gap on process-only task statements (plan mode, iterative refinement) is now explicitly the checkpoint tier's job, not a coverage bug. (2) Rewrote the checker in Python (`scripts/verify_module_01.py`), closing the portability/parsing/scoping/path-escape bugs structurally; found and fixed one further bug *in the rewrite itself* during re-verification (the CI-readiness check initially matched `SPEC.md`'s own description of the requirement, making it a silent no-op). (3) Added a compatibility contract and cumulative-gate convention to `fixtures/resolve/SPEC.md` — later modules may assume only what an earlier module's checker deterministically guarantees, and each module's checker will chain all prior modules' checkers starting with Module 02. (4) Reconciled the safety-rule contradiction: one canonical rule (escalate-when-unsure, the real exam scenario's own framing) stated once in `SPEC.md`, with `process_refund`-verification named as its Module 04 enforcement instance, not a second competing rule. (5) Property-phrased rubric criterion 2 (previously named the technique — `.claude/rules/` — directly, violating this workshop's own stated rule). (6) Reconciled the takeaway's "skills" promise (dropped; Module 01 doesn't produce a `.claude/skills/` artifact). (7) Added a 4th dry-run attempt (`weak-conceptual-attempt`): passes the deterministic tier completely while omitting the safety rule and using placeholder content — real, constructed evidence the conceptual tier catches something the deterministic tier structurally cannot, closing the exact validation gap the original dry run had flagged as untested. (8) Fixed `CLAUDE.md`'s unconditional "close every session" instruction, which caused Codex's read-only review agent to attempt running the closeout script mid-review (failed safely, no writes, but the instruction itself was the bug) — scoped to sessions that made changes. |
| 2026-07-15 | Module 02 authored with both tiers built together from the start | Explicit lesson from Module 01's remediation, not repeated this time. `scripts/verify_module_02.py` implements the cumulative-gate convention for real (imports and calls `check_module_01`). Deterministic tier is a real, provided pytest suite (`fixtures/resolve/tests/test_extraction.py`, 7 tests) against a learner-implemented `extraction.py`, using an injected `model_client` callable so the retry-loop logic is gradeable without a live API call. |
| 2026-07-15 | Dry run constructed 2 differently-shaped naive attempts, not just 1 | `no-retry-attempt` fails broadly/obviously (3/7 tests, a raised `KeyError`). `fabricates-attempt` fails narrowly/subtly (1/7) via a real Python footgun (`raw.get(x) or 0`, which collapses an honest `None` into a fabricated `0`) — directly exercises CCA-F Task Statement 4.3's own nullable-field/anti-fabrication skill. A bug was found in the dry run's own first construction of this attempt (used `raw.get(x, 0)`, the wrong pattern — only fires on an absent key, never exercised since the test always supplies an explicit `None`), caught and fixed before being recorded as a real finding — same "don't trust a check without testing it" discipline as Module 01's remediation, this time applied to a hand-constructed adversarial attempt rather than a script. |
| 2026-07-15 | **Doubt-driven-development review of Module 02, before treating it as done** | Same process as Module 01's (fresh Claude subagent + Codex CLI with live web-search access, given ARTIFACT+CONTRACT only, plus a Fable-model replan). Dominant finding, more severe than anything in Module 01's review: rubric criteria 3 and 4 graded a prompt/few-shot artifact the exercise's original interface never actually required a learner to produce — `model_client` was fully injected, so a learner doing exactly what was asked would write a validate/retry wrapper with zero prompt-engineering content anywhere in their submission. Full finding list kept in this session's transcript. |
| 2026-07-15 | **Structural fix: changed the exercise interface, not the rubric** | Fable's call, adopted: weakening criteria 3/4 would leave a module titled "Prompts and Structured Output" with no actual prompt engineering, and the stated takeaway (a few-shot/JSON-schema template) would be structurally unbuildable. Added `build_extraction_prompt(message, prior_attempts) -> str` and `FEW_SHOT_EXAMPLES` as required exercise deliverables; `extract_refund_request` now passes the constructed prompt, not the raw message, to `model_client`. Required zero changes to the original 7 tests (none asserted on call-tuple content), so the fix was additive, not a rewrite of already-validated behavior. |
| 2026-07-15 | **Remaining remediation applied same day** | Test suite grew from 7 to 14 tests, closing: an asserted-but-untested `"other"`-requires-detail rule; unvalidated `confidence` enum and `refund_amount_cents` type; an exhaustion test whose `<= 3` bound let a never-retrying implementation pass (tightened to `== 3`); a `prior_attempts` contract requiring a `"response"` key that was never checked; new tests directly verifying the prompt/few-shot artifacts exist and that a retry prompt literally embeds the specific prior error text. Checkpoint Q5's answer explanation now names `strict: true` as the real hard-guarantee mechanism (keeping the exam guide's own correct-answer ranking, adding technical precision Codex's live doc search found missing). Checkpoint Q11 reworded from "same model instance" to "same conversation/context" vs. "independent fresh request," fixing a real mental-model imprecision. README's self-contradictory "valid alternate terminal" (for an attempt that fails the stop condition two sections later) reworded to "expected intermediate state." Documented the Python 3.9+ requirement (`ModelClient`'s PEP 585 subscripting isn't covered by `from __future__ import annotations`). Added an explicit ARB-trigger-check-N/A line to `grading.md` (Module 02 only adds new files, doesn't touch Module 01's shipped config) — Coachgremlin's checklist requires this be evidenced, not merely true. Added a 4th dry-run attempt (`weak-few-shot-attempt`): passes all 14 tests with only clean, textbook-only few-shot examples — real, constructed evidence rubric criterion 3 catches what the 14-test deterministic suite structurally cannot. |
| 2026-07-15 | Module 03 authored, both tiers together, applying Modules 01-02's lessons proactively | `scripts/verify_module_03.py` chains Module 02's check (which chains Module 01's) via a real importable `check_module_02` function (refactored `verify_module_02.py` to expose one, matching Module 01's pattern, instead of shelling out via subprocess). Deterministic tier: a real 16-test pytest suite against the four MCP tools (`get_customer`, `lookup_order`, `process_refund`, `escalate_to_human`), each injected with a fake `Backend` (mirroring Module 02's `model_client` injection). Rubric criterion 2 (docstring keyword presence) was designed as deterministic from the start, not conceptual — applying the exact lesson Module 02's own review had to find after the fact. |
| 2026-07-15 | A real bug found in this module's own dry run, before any attempt was graded | Every tool's description was first written as a *module*-level docstring, not the *function's own* `__doc__` — real MCP tooling reads the function's docstring; a description that exists in the file but not on the function object wouldn't reach an agent either. `get_customer.__doc__`/`lookup_order.__doc__` came back empty strings against the first draft. Fixed by moving every tool's description into its function body, in both the shipped stubs and the dry-run reference implementation, before any naive attempt was constructed against the broken shape. |
| 2026-07-15 | Dry run constructed 2 differently-shaped attempts, closing the conceptual-tier gap proactively | `trusts-caller-attempt`: `process_refund` skips its own backend re-verification, reasoning "Module 04's hook already covers this" — fails exactly 1 of 16 tests. `weak-docstring-attempt`: correct behavior, but docstrings are minimal ("Gets a customer...") — technically contains the required disambiguating keywords (passing the deterministic presence check) while giving an agent no real boundary reasoning; passes all 16 tests. Constructed *before* shipping, not found afterward by an adversarial review — the first time this pipeline has applied that discipline proactively rather than reactively. |
| 2026-07-15 | **Doubt-driven-development review of Module 03, before treating it as done** | Same process as Modules 01-02's (fresh Claude subagent + Codex with live repo exploration + Fable replan). Found two issues more severe than either prior review: (1) the cumulative-gate chain into Module 04 was already broken — `check_module_01` returns bare `CheckResult`, `check_module_02` returns `tuple[CheckResult, str]`, and Module 03's first draft exposed no `check_module_03` at all; (2) both `verify_module_02.py` and the first `verify_module_03.py` ran the *submission's own copy* of the test file, meaning a learner could weaken or delete `tests/test_extraction.py`/`tests/test_tools.py` and pass trivially — the "don't edit this" notice was prose, not enforcement. |
| 2026-07-15 | **Structural fixes: unified checker interface + canonical-test execution** | Every `check_module_NN(target) -> CheckResult` now returns the same shape (`CheckResult` gained an `output: str` field); each module's `main()` calls its own `check_module_NN` instead of duplicating logic; `check_module_03` added. Both `verify_module_02.py` and `verify_module_03.py` now copy the *repo's own canonical* test file to a temp location and run that, with `cwd` set to the submission so imports still resolve — verified empirically that legitimate submissions are unaffected and that a tampered test file no longer helps a broken submission pass (re-tested for both Module 02 and Module 03). Convention documented in `fixtures/resolve/SPEC.md`'s compatibility contract. |
| 2026-07-15 | **Remaining Module 03 remediation applied same day** | Test suite grew from 16 to 20 tests: `process_refund`'s docstring is now checked (previously only 2 of 4 tools' docstrings were, despite the rubric claiming all four); a new test asserts `find_customer` is called before `find_order`, and that `find_order` is never called at all when the customer doesn't verify — turning the "re-verify BEFORE doing anything else" claim from prose into a gate; `ERROR_CATEGORIES` gained documented semantics (transient/validation/business/permission definitions) and became a tuple; `escalate_to_human` is now documented and tested as this project's deliberate fail-open path (a new test asserts an *empty* backend still succeeds, the opposite assertion from `process_refund`'s); the order-leak test tightened from a string-absence check to an equality assertion against a genuinely-nonexistent-order result; `lookup_order`'s docstring had exam/exercise meta-commentary stripped (a real tool description wouldn't reference "the exam guide's Sample Question 2"); the README's `.mcp.json` takeaway promise was reworded since the exercise doesn't produce one; two checkpoint answers (Q8, Q12) gained precision caveats; `backend.py` now explicitly states that refund *execution* (persistence, idempotency) is deferred to Module 04, distinct from the refund *decision* this module verifies — a stated deferral, not a silent gap. |
| 2026-07-15 | Module 04 authored, both tiers together, applying Modules 01-03's lessons proactively | `scripts/verify_module_04.py` chains Module 03's check (which chains Module 02's, which chains Module 01's) via a real importable `check_module_03`/`check_module_04` function. Deterministic tier: a real 12-test pytest suite against `src/agent.py`'s `run_support_session` (the agentic loop, keyed strictly on `stop_reason`) and `verify_before_refund_hook` (a session-level, `PreToolUse`-style hook blocking `process_refund` until `get_customer` has *succeeded* in the same session). Tests use spy tool functions rather than Module 03's real tool implementations — this module's exercise is the loop and the hook in isolation, not re-proving Module 03's already-tested tool behavior. |
| 2026-07-15 | **Resolved the deferred stateful-backend question from Module 03** | `backend.py`'s docstring, written during Module 03, stated refund *execution* (persistence, idempotency) was "explicitly Module 04's exercise." Decision: Module 04 does **not** extend `Backend` into a stateful, mutating protocol. Its exercise is orchestration (the loop and the hook), which maps to Domain 1 (agentic orchestration); refund persistence/idempotency are Domain 2 (tool design) territory Module 03 already resolved. `process_refund`'s "success" response continues to mean "decision verified," not "money moved," for the rest of this project. `backend.py`'s docstring rewritten to state this directly instead of pointing at a module that was never going to pick it up. |
| 2026-07-15 | Dry run constructed 4 differently-shaped attempts, one closing the conceptual-tier gap proactively | `no-hook-attempt`: the loop never calls `verify_before_refund_hook` before dispatch — passes the hook's own isolated unit tests but fails both integration tests that exercise the hook's effect through the loop (2/12 fail), evidence that a unit-test-only suite would have missed this class of bug. `text-parsing-attempt`: terminates early on wrap-up-sounding response text despite `stop_reason: "tool_use"` — the documented anti-pattern (1/12 fail). `trusts-attempted-call-attempt`: records a tool call as verification whenever the tool was *called*, regardless of whether it *succeeded* — a failed `get_customer` (customer not found) still unblocks `process_refund` (1/12 fail). `thin-docstring-attempt`: behaviorally identical to the correct implementation (12/12 pass) but with all architectural reasoning stripped from docstrings and a minimal (though deterministically-sufficient) rejection message — constructed *before* shipping to isolate rubric criterion 2, applying the exact "close the conceptual gap proactively" lesson Modules 01-03 each had to learn via an external doubt-driven-development review first. |
| 2026-07-15 | **Doubt-driven-development review of Module 04 (fresh Claude subagent + Codex, both live-reproducing findings against the actual checker), before treating it as done** | Both independently found, and Codex confirmed/deepened, the same dominant issue: `verify_before_refund_hook`'s original design blocked on "did *any* `get_customer` call succeed in this session," never checking *which* customer it verified — a session verifying customer A could refund customer B, and none of the 5 dry-run attempts constructed beforehand happened to probe this. Codex additionally confirmed a hook blocking on "any tool succeeded" (not `get_customer` specifically) also passed all 12 original tests. Both reviewers reproduced the gap live against `scripts/verify_module_04.py`, not just by code inspection. |
| 2026-07-15 | **Fixed: identity-bound hook enforcement** | `SessionState` gained `verified_customer_id`, set only from a *successful* `get_customer` call's actual result (the backend-verified `customer_id`, not the raw `identifier` the caller asked to look up). `verify_before_refund_hook` now requires `process_refund`'s `customer_id` to match it. Test suite grew from 12 to 16 tests: a hook-unit customer-mismatch test, an integration-level customer-mismatch test, a "different tool succeeded, not `get_customer`" test, and an `escalate_to_human` loop-level integration test (previously hook-unit-level only). The flawed attempt `trusts-attempted-call-attempt` was renamed `trusts-identifier-attempt` and redesigned around the surviving, more realistic version of its original bug (conflating the requested identifier with the verified customer_id) — its original form stopped being observable once error responses (which never carry a `customer_id` key) made the old distinction invisible to the suite. |
| 2026-07-15 | **Fixed: 4 further real issues from the same review** | (1) The spy tool test doubles accepted `backend` via `**kwargs` and silently tolerated its absence — an implementation omitting `backend=backend` (required by the real Module 03 tools) could still pass; fixed by making the spy tools' `backend` parameter keyword-only and required, verified empirically against a constructed submission that omits it (`TypeError`, 9 tests fail). (2) `checkpoint.md` Q14's answer key overclaimed that session forking isolates a branch's real-world tool-call side effects — factually wrong for the real Claude Agent SDK model; rewritten so the misconception is a wrong answer, not the correct one. (3) Module 03's own README still promised refund persistence/idempotency was "explicitly Module 04's exercise," contradicting the scope decision Module 04 actually made — fixed in Module 03's live README (its own frozen dry-run history from 2026-07-15 is left as a point-in-time record, per this project's append-only-ADR discipline). (4) `checkpoint.md` stated 80% as `12/14`; the README stated `11/14` (an arithmetic error — 11.2 rounds up to 12); fixed to match. Also tightened: `max_iterations` exhaustion now returns a specific required sentinel (`stop_reason: "max_iterations"`), not merely "not end_turn," closing a gap between the README's claim and what was actually tested. |
| 2026-07-15 | **Fable-model critique of the remediation itself (not the original findings), before treating Module 04 as done** | Standing practice for this arc: after reconciling doubt-driven-development findings and applying fixes, a Fable-model subagent reviewed the *actual current files* (not a summary) to check the fix was complete, correctly scoped, and hadn't over-corrected. Verdict: sound at its core (every claim reproduced exactly on independent re-run, no scope creep) but incomplete at the documentation edges. |
| 2026-07-15 | **Fixed: 3 gaps Fable's critique found in the remediation** | (1) `fixtures/resolve/SPEC.md` and the shipped `src/tools/process_refund.py` (plus its copies in all 5 Module 04 attempt directories — active supporting infrastructure for this module's own dry run, not frozen history) still stated the pre-fix "any get_customer success" rule with no customer-identity binding; rewritten to name the binding explicitly. (2) A stale quotation of the pre-fix docstring wording survived inside the shipped `tests/test_agent.py` itself; fixed. (3) `trusts-identifier-attempt` was only caught in the false-*block* direction (over-blocking a legitimate refund) — no test exercised the false-*allow* direction the bug actually reintroduces. Added a 17th test (`get_customer` resolves identifier "cust_1" to a *different* real customer_id "cust_42"; a refund then requested for "cust_1" must still be blocked) — `trusts-identifier-attempt` now fails 2/17, both directions of its bug caught. One further finding (a session holding at most one verified customer at a time, last-verified-wins) was already fail-closed and correct; documented with a docstring sentence, no mechanism change, since a fix there would have been over-correction. |
| 2026-07-16 | Module 05 authored (case facts + escalation decision), both tiers together | `scripts/verify_module_05.py` chains Module 04's check for real (`check_module_04`/`check_module_05`). Deterministic tier: a real 20-test pytest suite against `src/context.py`'s `update_case_facts` (sourced, conflict-annotated case-fact extraction from tool results) and `should_escalate` (a structured escalation decision keyed to conflicts/error-count/iteration-proximity, deliberately with no confidence parameter in its signature). Two new files only — `context.py`, `test_context.py` — no prior module's shipped stub or test file touched, confirmed by full regression across all 16 prior dry-run attempts (Modules 01-04) after adding them. |
| 2026-07-16 | Scoping decision: Task Statements 5.3/5.4 have no `resolve`-specific artifact yet | `resolve` has no subagents (multi-agent error propagation, 5.3) and isn't a large-codebase-under-exploration scenario (scratchpad/`/compact` discipline, 5.4) — both tested in the closed-book checkpoint only, the same division-of-labor convention every prior module has used for task statements without a natural artifact. 5.1, 5.2, 5.5, 5.6 get the real `context.py` artifact. |
| 2026-07-16 | Stated explicitly: this module's conflict-preserving design is not a contradiction of Module 04's overwrite-on-verify design | Module 04's `SessionState.verified_customer_id` is last-verified-wins by design (real-time authorization, one active verified customer at a time). This module's `CaseFacts.conflicts` preserves disagreements instead (a reliability/audit concern, not an authorization one) — both correct for what each is for; `context.py`'s own docstring says so directly rather than leaving a learner to reconcile the apparent tension unaided. |
| 2026-07-16 | Dry run constructed 4 flawed attempts plus a proactively-built conceptually-weak one, continuing Module 04's established discipline | `silent-overwrite-attempt` (never records a conflict), `request-trusting-attempt` (originally: reads `process_refund`'s requested amount instead of the actual refunded amount), `confidence-proxy-attempt` (ignores `case_facts` entirely, using iteration count alone as an unreliable proxy — the same anti-pattern Task Statement 5.5 names, here self-inflicted rather than model-inflicted), `thin-docstring-attempt` (passes every test, zero documented reasoning — built before any external review, same discipline as Module 04's own `thin-docstring-attempt`). A gap found in the test suite's own first draft (no test for two *independent* conflicts across different fields accumulating correctly) was closed before shipping, growing the suite from 19 to 20 tests. |
| 2026-07-16 | **Doubt-driven-development review of Module 05 (fresh Claude subagent + Codex, both constructing and running live counter-implementations against the real checker), before treating it as done** | Both found the same dominant issue, Codex deepening it substantially: the test suite tested `customer_id` far more rigorously than the structurally-identical `order_id`/`refund_amount_cents` mappings — a request-vs-result mixup, a hollow (unsourced) conflict entry, or a silently-overwritten conflict on either of those two fields all passed the original 20 tests. Both reviewers constructed and ran the exact counter-implementations to confirm this, not just identified the gap by inspection. |
| 2026-07-16 | **Fixed: symmetric test coverage across all three mapped fields** | Added the missing request-vs-result test for `lookup_order`, conflict-preservation and no-conflict-on-repeat tests for `order_id`, and a conflict test for `refund_amount_cents`. `request-trusting-attempt` extended to also misread `lookup_order`'s `order_id` (previously only `process_refund`), so the dry run's flaw matrix represents the contract's full scope. Test suite grew from 20 to 28. |
| 2026-07-16 | **Fixed: 2 further structural gaps, each verified against a live counter-implementation** | (1) The non-mutation test only checked scalar-field reassignment; a `dataclasses.replace(case_facts)` shallow copy (sharing the same `conflicts` list object) passed all 20 original tests while still corrupting the caller's original once a later conflict was appended — fixed with a test checking the returned `conflicts` list is independent of the original's, plus a companion test that a failed call neither discards existing facts nor mutates the original object; both verified against live counter-implementations (a shallow-copy implementation and an in-place-mutating error path) that fail exactly the new tests. (2) `should_escalate`'s "no confidence parameter" guarantee was asserted in prose, never checked — an implementation adding an unused `confidence: float = None` parameter passed all 20 original tests; fixed with `inspect.signature`-based test, verified against a live counter-implementation with the added parameter. |
| 2026-07-16 | **Fixed: 2 smaller findings** | `CaseFacts.issue_summary` was a dead field (no `FACT_SOURCES` mapping, no test, no explanation, and a structural risk as an undetected fourth `should_escalate` trigger) — removed entirely rather than merely documented, from the shipped stub, reference implementation, and all five dry-run attempts. `should_escalate`'s iteration-proximity check had no test at the exact `max_iterations - 1` boundary (only "near" and "well before"); an off-by-one implementation (`- 2` instead of `- 1`) passed all 20 original tests — fixed with a boundary test, verified against a live off-by-one counter-implementation. Also: checkpoint Q12's answer key overclaimed that `source_tool` alone can distinguish two independent calls to the same tool from a single re-report (it structurally cannot — `CaseFact` records no call ID or timestamp) — rewritten so the overclaim is a wrong answer, not the correct one. |
| 2026-07-16 | **Fable-model critique of the remediation itself, before treating Module 05 as done** | Standing practice continued from Module 04: after reconciling the doubt-driven-development findings and applying fixes, a Fable-model subagent read the actual current files (not a summary) to check the fix was complete. Verdict: honestly executed, no scope creep, but incomplete against its own dominant finding — the "symmetric coverage" fix covered the specific counter-implementations the first review happened to construct, not the full guarantee-by-field matrix `order_id`/`refund_amount_cents` needed to actually match `customer_id`'s existing rigor. |
| 2026-07-16 | **Fixed: 4 gaps Fable's critique found in the "symmetric coverage" remediation, each verified against a new live counter-implementation** | (1) Conflict-preservation tests for `order_id`/`refund_amount_cents` asserted values but never `source_tool` — a hollow, unsourced conflict entry for either field passed 28/28; fixed by adding `source_tool` assertions to both tests. (2) No test exercised a *failed* `lookup_order` call with real args carrying an `order_id` key — an implementation extracting a fact from the request on the error path specifically (the same key-collision the success-path fix had just closed, reopened one path over) passed 28/28; fixed with a dedicated test. (3) Most-recent-wins was untested for `refund_amount_cents` conflicts — an implementation recording the conflict correctly but keeping the *old* amount as current passed 28/28; fixed by asserting the current value in the existing test. (4) No-conflict-on-repeat was untested for `refund_amount_cents` — given `should_escalate`'s conflicts-first precedence, a spurious refund conflict would wrongly escalate a fine session; fixed with a new test. Test suite grew from 28 to 30. |
| 2026-07-16 | Module 06 (Foundations Capstone) authored with a different exercise shape from every prior module: diagnose-and-fix, not stub-implementation | `fixtures/resolve/src/session.py` ships fully written and running (not `raise NotImplementedError`), integrating Module 02's extraction, Module 04's loop/hook, and Module 05's case facts/escalation into `run_full_support_session` — with 2 real, seeded defects already in place. `scripts/verify_module_06.py` chains `check_module_05`; the provided 5-test suite fails 2/5 against the exact shipped file, matching CCA-F's own scenario-based format (finding a defect in a mostly-working system, not building from nothing). |
| 2026-07-16 | The two seeded defects, chosen to each span a different pair of prior modules | (1) `ExtractionResult.confidence` (Module 02, a legitimate extraction-quality signal) fed into a parallel, unofficial escalation check bypassing `should_escalate` (Module 05) entirely — reintroducing the unreliable-signal anti-pattern Module 05's own signature was built to structurally prevent, via a side door instead of through the function itself. (2) `should_escalate` checked against `facts` *before* `update_case_facts` folded in the current turn's tool result — a one-turn-stale escalation decision, the same shape of bug Module 04 and Module 05's own doubt-driven-development reviews each independently found and fixed in their own code. |
| 2026-07-16 | Dry run constructed 4 flawed attempts isolating each defect independently, plus an over-correction case | `unfixed-attempt` (both defects present, baseline), `fix-bug1-only-attempt`/`fix-bug2-only-attempt` (each fixes exactly one defect, proving the two are genuinely independent), `over-correction-attempt` (deletes the extraction step entirely rather than fixing only its misuse — passes the defect-1 test vacuously while failing a separate test that specifically checks extraction still runs). |
| 2026-07-16 | Mock exam checkpoint authored as an original 6-scenario pool (4-of-6 draw), not the real exam's own scenarios | 42 originally-written questions (7 per scenario), a documented, honest reduction from the real exam's ~60-question total for a 4-scenario draw — the 6-pool/4-draw structure and 720/1000-equivalent scoring are matched faithfully; the item count is not claimed to match exactly. None of the 6 scenarios are the real CCA-F exam's own published scenario names/content. |
| 2026-07-16 | **Doubt-driven-development review of Module 06 (fresh Claude subagent + Codex, both actually executing the checker against every attempt), before treating it as done** | Claude subagent's dominant finding: all 42 checkpoint answer-key entries named option A as correct, with no exceptions — a test-taker could score 1000/1000 without reading a single question. Codex's dominant finding, more severe: the "correct" reference implementation itself never escalates when the hook repeatedly rejects a call, contradicting this project's own fail-closed design default — reproduced live against `correct-attempt` (3 blocked refunds → silent `max_iterations`, no escalation). |
| 2026-07-16 | **Fixed: checkpoint answer-key shuffle, mechanically verified** | Every question's 4 options rotated via a seeded, per-question rotation targeting an even ~10-11-per-letter distribution (final: A:10, B:11, C:11, D:10); a script re-parsed the transformed file confirming each question's originally-correct option text now sits at the letter its answer key claims, and confirming each answer key's 3 mentioned distractor letters exactly match the 3 non-correct letters, for all 42 questions. |
| 2026-07-16 | **Fixed: 3 scenarios renamed to reduce resemblance to the real exam's own published scenarios** | Scenario C (code-review-in-CI bot ~ "Claude Code for CI") → a release-notes drafting agent in a deploy pipeline. Scenario D (research aggregator over parallel subagents ~ "Multi-Agent Research System") → a multi-channel customer feedback synthesizer. Scenario F (onboarding assistant for new engineers ~ "Developer Productivity with Claude") → a legacy-codebase documentation generator. Every scenario-specific noun reference throughout each scenario's 7 questions updated to match. Two further gaps disclosed rather than hidden: Task Statement 5.4 has zero questions in this 42-question pool (covered in Module 05's own checkpoint instead); Domain 3 (10 questions) and Domain 4 (7) have equal real weight but unequal pool representation, an artifact of the fixed 7-per-scenario count. |
| 2026-07-16 | **Fixed: the rejection-path escalation gap Codex found, uniformly across all six copies of `session.py`** | A hook rejection now also calls `should_escalate` before continuing the loop, rather than an unconditional `continue` that skipped Module 05's escalation logic entirely. This was a genuine gap in the *base design* shared identically by the shipped stub and all five dry-run attempts — not one of the two seeded pedagogical defects, since neither defect's fix touches the rejection path. A 6th test (`test_repeated_hook_rejections_eventually_escalate`) checks this directly; test suite grew from 5 to 6. Also fixed: the checkpoint's "none of these 6 scenarios are the real exam's own" claim, reworded to disclose Scenario A's deliberate, admitted parallel to the real exam's own "Customer Support Resolution Agent" scenario (already stated elsewhere in `fixtures/resolve/SPEC.md`) rather than contradicting it. |
| 2026-07-16 | **Fable-model critique of the remediation itself, before treating Module 06 as done** | Standing practice continued from Modules 04-05: a Fable-model subagent read the actual current files and built its own mutants rather than trusting the write-up. Verdict: substantively sound (rejection-path fix correct and uniform, answer-key shuffle genuinely verified, no scope creep) but the new 6th test had a real precision gap, and two scenario-renaming sweeps (Module 06's own remediation from earlier the same day) were incomplete. |
| 2026-07-16 | **Fixed: 3 gaps Fable's critique found, each verified against a constructed mutant** | (1) The 6th test (`test_repeated_hook_rejections_eventually_escalate`) never checked that escalation *doesn't* happen prematurely — a mutant escalating unconditionally on the first rejection (ignoring iteration budget, never calling `should_escalate` at all) passed all 6 tests; fixed by asserting the model was consulted twice before escalating, and by asserting the hook-blocks test reaches a normal `end_turn` rather than escalating on one rejection alone. Verified against Fable's own constructed mutant. (2) Scenario C's and F's renaming (done earlier the same day) left stale vocabulary from the old framing in 5 distractor options (not the correct answers) — "reviewed"/"review" language for the release-notes agent, a "new engineers' first week" distractor for the documentation generator — reworded to match each scenario's actual premise. (3) Scenario B's B7 stem described a generic "PR review comment" scenario disjoint from its own premise (a documentation search assistant) — reworded to be about the assistant's own citation-extraction code being checked in CI. |
| 2026-07-17 | **Part 2 shared project: new system, not a `resolve` continuation** | `fixtures/resolve/SPEC.md` left this as an explicitly open question. Presented via `AskUserQuestion`: coderturtle chose "New shared system for Part 2" over the recommended "Anchor on resolve, artifacts not code" and "Keep extending resolve with real code." Reason surfaced in the second question's framing and confirmed by the choice: `resolve` (a single customer-support agent) has no natural home for CCAR-P Domain 7 (Developer Productivity & Operational Enablement) without contortion, and stretching one customer-facing product across all 7 Professional domains would force artificial scenarios in at least one module. |
| 2026-07-17 | **Part 2 scenario: Foundry, an internal AI platform team's own product** | Second `AskUserQuestion`: coderturtle chose the recommended "Internal AI platform team" over "Enterprise SaaS product scaling up" and "resolve, scaled to enterprise." An internal platform team naturally serves many different internal requesting teams with genuinely different problem shapes each module, which is what makes Domain 7 (and the other 6 domains) each land on a real, distinct artifact instead of a stretched one. Named Foundry; full rationale and module build-out table in the new `fixtures/foundry/SPEC.md`. |
| 2026-07-17 | **Compatibility-contract deviation for Part 2, stated explicitly and disclosed in both `SPEC.md` and the checker's own docstring** | `foundry`'s modules chain every prior *Part 2* module's checker (`verify_module_08` will call `check_module_07`), but do not chain back through Part 1's `check_module_06`/`resolve` gate. Module 06's own hard-prerequisite framing already establishes Part 1 completion for any learner reaching Module 07; `foundry` is a structurally separate codebase from `resolve`, so re-validating six already-passed Part 1 gates against an unrelated fixture would test nothing real. `check_module_07` plays the same role for Part 2 that `check_module_01` played for Part 1. |
| 2026-07-17 | Module 07 authored (ticket-triage classifier + ADR), both tiers together, opening Part 2 | `scripts/verify_module_07.py` is a fresh starting point (no `check_module_06` import) validating two structurally different deliverables in one gate: a real 12-test pytest suite against `src/ticket_triage.py`'s `build_triage_prompt`/`classify_ticket`, and a regex-based structural check of `docs/adr-0001-ticket-triage-architecture.md` (4 required sections, minimum content length per section, a keyword check that "Alternatives Considered" names the agentic/`resolve` alternative). The exercise's real design tension: the Helpdesk team's own first instinct ("we want something like `resolve`") is what the exercise tests against — most of their ~4,000 tickets/month need single-turn classification, not an agentic loop; the CCAR-P skill under test is recognizing when *not* to reach for the more powerful pattern. |
| 2026-07-17 | Cache-friendliness designed as a property distinct from, and contrasted with, Module 02's own retry pattern | `build_triage_prompt()` takes no arguments and must return a byte-identical string on every call; `classify_ticket`'s `system_prompt` argument to `model_client` must stay identical across all calls, including retries — retry-specific validation-error feedback travels through `turn_content` (the dynamic per-call argument), never by mutating the system prompt. This is the opposite of Module 02's `extract_refund_request`, which regenerates its whole prompt (including the error) on every retry attempt — correct there (not sent at cache-relevant volume), wrong here (sent ~4,000 times/month against the same static policy text). Anchored to the CCA-F/CCAR-P exam guide's own Sample Question 2 (a large, repeatedly-sent static system prompt). Verified via a dedicated dry-run attempt (`regenerates-prompt-on-retry-attempt`) that mirrors Module 02's pattern here specifically to confirm it fails only the retry-specific cache test (1/12), distinct from the cruder `system-prompt-mixes-ticket-attempt` (fails 2/12, the two call-invariance tests). |
| 2026-07-17 | Dry run constructed 5 attempts (1 correct, 4 flawed), all confirmed via live checker execution on first construction | `no-retry-attempt` (skips retries entirely, fails 5/12 — every test depending on retry behavior), `system-prompt-mixes-ticket-attempt` (concatenates ticket text into the "static" prompt, fails 2/12 — the two call-invariance tests), `regenerates-prompt-on-retry-attempt` (Module 02's pattern applied here, fails 1/12 — the retry-specific cache test only), `weak-adr-attempt` (fully correct code, 12/12, but a 3-section ADR missing "Alternatives Considered" entirely — fails the ADR check, confirming the two halves of the gate are independently enforced). Full write-up in `runs/2026-07-17-module-07-dry-run/grading.md`. |
| 2026-07-17 | Checkpoint cited by domain objective name, not invented task-statement numbers | `docs/workshop-design.md` has no granular numbered breakdown for CCAR-P Domain 1/Domain 2 (unlike CCA-F's Domain 1 "Task Statement 1.1"–"1.7" convention used throughout every Part 1 checkpoint) — confirmed via `grep` before writing, no such breakdown found anywhere in the repo's design docs. Rather than fabricate a decimal numbering scheme that would falsely imply verification against Anthropic's real exam guide, `checkpoint.md`'s 14 questions cite each domain's own named objective instead (e.g., "Domain 1: architectural pattern selection"), drawn directly from `docs/workshop-design.md`'s own listed objectives for these two domains — a disclosed, deliberate citation-style difference from every Part 1 checkpoint, stated in the checkpoint's own header. Questions split 8/6 across Domain 1/2's four objectives each, proportionally matching the real 17%/13% weight split, and use two new scenarios (Sales Engineering RFP drafting, Security alert remediation) beyond the hands-on exercise's own Helpdesk scenario so a learner can't pass by pattern-matching one example. |
| 2026-07-17 | **Doubt-driven-development review of Module 07 (fresh Claude subagent + Codex cross-model + Fable-model critique of the remediation), before treating it as done — the most consequential review this project has run to date** | Stage 1 (Claude subagent, adversarial review of ARTIFACT+CONTRACT only): 10 findings, most severe being a retry-content test satisfiable by generic "invalid" boilerplate instead of the specific validation error, the documented default `max_retries=2` never actually exercised by any test, no test against a repeated identical ticket (a memoizing implementation could skip calling `model_client` undetected), and whitespace-only `detail` passing a naive truthiness check. Stage 2 (Codex, `codex exec --sandbox read-only`, same ARTIFACT+CONTRACT): confirmed several Stage 1 findings and surfaced the dominant issue — `run_triage_tests` and the identically-shaped function in all five already-merged Part 1 checkers (`verify_module_02.py`–`verify_module_06.py`) ran pytest with `cwd=target`, letting a submission's own top-level `pytest.py` shadow the real installed pytest package (`python -m pytest` puts cwd at the front of `sys.path`) and skip the entire canonical test file while still reporting a clean pass. **Empirically confirmed live**, not just by inspection: Module 04's `correct-attempt` with its `agent.py` swapped for the known-broken `no-hook-attempt` version (missing the safety hook Module 04's own earlier doubt-driven-development had found) plus a shadow `pytest.py` produced a clean `Deterministic tier: PASS`. |
| 2026-07-17 | **Fixed: neutral-temp-dir test execution across all six checkers** | `verify_module_02.py` through `verify_module_07.py`'s test-execution functions now copy the submission's `src/` directory into a fresh, neutral temp directory and run pytest with `cwd` set to *that* directory, never `cwd=target`. Re-verified: the Module 04 bypass attempt above now correctly fails; all 25 Part 1 dry-run attempts and all 5 Module 07 attempts reproduce their exact prior pass/fail pattern (zero regression). |
| 2026-07-17 | **Stage 3: Fable-model critique of the remediation itself found the fix closed one vector, not the underlying class of bug** | Submission code still executes during pytest's own collection step (importing `src/ticket_triage.py` is exactly that), so a submission with `os._exit(0)` at module import time kills the whole pytest subprocess with return code 0 before a single test runs — the checker's `returncode == 0` check alone read that as a clean pass. **Empirically confirmed live**: `correct-attempt` with `src/ticket_triage.py` replaced by `import os; os._exit(0)` produced `Deterministic tier: PASS`, on both Module 04's and Module 07's checkers. Fable also found a hardcoded `for _ in range(3):` (ignoring `max_retries` except the negative-value guard) would still pass all 19 tests at that point, that the ADR fence-stripping mitigation (added mid-review) could falsely delete real content across an unclosed code fence, and that several new docstrings cited a "2026-07-17 decisions entry" that didn't exist yet at review time. |
| 2026-07-17 | **Fixed: expected-pass-count verification, closing the `os._exit` class of bypass across all six checkers** | Each checker now additionally requires the pytest summary output itself report the full expected test count passed (computed by counting `^def test_` in the canonical test file, checked as the literal substring `f"{expected} passed"` in stdout+stderr), not just a zero exit code — `os._exit` truncates output before that summary line is ever printed, so it can no longer read as a pass. Re-verified live on both Module 04's and Module 07's checkers; `correct-attempt` still passes. Also fixed: a 20th test (`test_classify_ticket_respects_a_non_default_max_retries`, `max_retries=4`, expecting exactly 5 calls) distinguishing "respects the argument" from "hardcoded to match the default"; the ADR fence-strip regex now only fires when `` ``` `` markers appear in balanced count, so an unclosed fence can no longer make it delete real section content — the remaining gap (a fake heading inside a non-```` ``` ```` fence) is disclosed as a known limit of a coarse regex proxy, not silently assumed solved. Third review cycle (Fable re-reading the second remediation) surfaced only already-anticipated documentation staleness, closed by this same documentation pass — stop condition met. |
| 2026-07-17 | **Fixed: remaining Stage 1/2 findings** | Retry-content test now requires the literal invalid category value, not a generic placeholder, and additionally checks the original ticket text survives into the retry's turn content; default-`max_retries` and repeated-identical-ticket gaps each closed with a dedicated test; whitespace-only `detail` now rejected by `_validate` (`.strip()`); non-dict `model_client` responses now treated as a retryable validation failure instead of crashing; `max_retries < 0` now raises `ValueError` immediately, before any `model_client` call, instead of falling through to an `IndexError`; `TICKET_CATEGORIES` converted from a list to a tuple, matching `fixtures/resolve/src/tool_errors.py`'s `ERROR_CATEGORIES` precedent; `build_triage_prompt` given a real content floor (must mention all five category names) since a prompt as thin as `return "classify"` previously satisfied every test. Test suite grew from 12 to 20. All four flawed dry-run attempts and `weak-adr-attempt` re-synced with the shared base fixes so each still fails the new suite for exactly its own original, specific, intended flaw — re-verified with clean isolation. Full write-up in `runs/2026-07-17-module-07-dry-run/grading.md`. |
